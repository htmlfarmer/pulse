# Example environment variables for running the trigger server and deploys
# Copy this to .env and fill in secure values. Never commit your .env with secrets to git.

# Secret token used to authorize trigger requests from the local UI or admin page
TRIGGER_SECRET=replace_with_a_random_secret_string

# Optional: path to virtualenv used by scripts/run_and_deploy.sh
# VENV=.venv
